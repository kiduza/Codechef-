#include<iostream>
using namespace std;
int main(){
    int T, A, B;
    cin>>T;
    while(T--){
        cin>>A>>B;
        if((A == 0) && (B != 0)){
            cout<<"Liquid"<<"\n";
        }
        else if((B == 0) && (A != 0)){
            cout<<"Solid"<<"\n";
        }
        else if((B != 0) && (A != 0)){
            cout<<"Solution"<<"\n";
        }
    }
    return 0;
}
